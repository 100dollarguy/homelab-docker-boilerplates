IMAGE=jc21/nginx-proxy-manager:latest
CONTAINER_NAME=nginx-proxy-manager

HTTP_PORT=80
ADMIN_PORT=81
HTTPS_PORT=443

# This stack CREATES the proxy network — all other stacks join it as external
NETWORK=proxy_net
